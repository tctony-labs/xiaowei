# 设置窗口 UI 清单与 Storybook 范围

## Why

用户要求先核对旧版设置窗口并列出 UI 清单，随后确认除扩展外均制作 Storybook。当前仅制作设置 UI 预览，不实施设置业务，不依赖 gateway、统一数据库或 setting 服务。

## What

用户已确认制作除扩展外的全部页面及相关弹窗 Storybook；扩展不实施。以下保留完整旧版源码清单供对照。Storybook 复用纯展示组件和模拟数据，逐项确认后才能接入实际页面与业务。

参考仓库：`~/Develop/XiaoWei/workspace/src/xiaowei-next`。入口为 `xiaowei/src/pages/SettingsPage.tsx`，下表源码均相对该仓库的 `xiaowei/src/components/settings/`。

| 编号 | 页面／区域 | 旧版 UI 内容 | Storybook 重点状态与交互 | 源码 |
| --- | --- | --- | --- | --- |
| 0 | 窗口框架 | 左侧 207px 导航、8 个页签、固定页标题、内容区滚动、顶部 32px 拖拽区、初始化加载、设置项定位高亮 | 明暗主题、页签切换、长内容滚动、跳转定位高亮；原生拖拽另行验收 | `SettingsLayout.tsx`、`SettingsTabLayout.tsx`、`SettingsAnchor.tsx` |
| 1 | 通用 | 账号卡片；主题（跟随系统／浅色／深色）；开机自启动；全局搜索包含浏览器书签 | 未登录、等待扫码、登录失败；头像／昵称／UIN 复制／退出；未连接、连接中、已连接、登录失效；开关与主题选择 | `GeneralTab.tsx`、`AccountSection.tsx` |
| 2 | 快捷键 | 全局搜索、快速对话、剪贴板 | 已设置、未设置、录制按键、清除、Esc 取消；重复快捷键转移行为 | `ShortcutTab.tsx`、`ShortcutSettingsCard.tsx`，以及 `../ShortcutInput.tsx` |
| 3 | 剪贴板 | 启用记录、自动粘贴、图片内容提取、保留期限（1／7／15／30 天／永久）、已用空间与刷新 | 开关；图片提取已启用／跳转模型页；空间计算中；清理中／完成／失败重试 | `ClipboardTab.tsx` |
| 4a | 模型：默认选择 | 默认模型、小文本任务模型、默认思考模式、图片生成模型 | 无可用模型、已配置模型；思考强度关闭／较小／中等／较大／极大；图片模型验证中／成功／失败 | `LlmTab.tsx`、`DefaultModelSection.tsx` |
| 4b | 模型：提供商 | 提供商列表与选择；添加／修改／删除 | 提供方下拉（首项自定义）、固定／可编辑地址及协议、获取模型列表、默认模型、接口上下文上限与手动覆盖；获取中、空列表、失败重试、保存及删除确认 | `ApiKeySection.tsx`、`ApiKeyModal.tsx`、`ContextWindowField.tsx` |
| 4c | 模型：本地模型 | 启用开关、当前模型、切换弹窗、磁盘占用、打开模型目录 | 关闭／展开；未下载、下载进度、校验中、解压中、已就绪、失败；模型选择 | `LocalModelSection.tsx`、`BaseModelPickerModal.tsx`、`BaseModelRow.tsx`、`ModelStateBadge.tsx` |
| 5 | 智能体 | 单轮工具调用上限（30／50／100／300）；网络搜索源 Bing／Tavily／Brave／Serper；Key 配置和测试 | 未配置 Key、配置／修改／删除 Key 弹窗、删除确认、测试中／成功／失败、设置项高亮 | `AgentTab.tsx`、`WebSearchSection.tsx`、`WebSearchApiKeyModal.tsx` |
| 6a | 扩展：列表与详情 | 生成／新建／导入入口；列表图标和状态；详情返回、调试、本地发布、目录与解释器、触发配置、环境变量、发布管理、开发者信息 | 加载、空列表、错误、无效／已发布／调试中；详情切换、目录丢失、配置错误、禁用动作 | `ExtensionsTab.tsx` |
| 6b | 扩展：弹窗 | 新建、导入、删除 | Script／UI 类型；Bash／AppleScript／Python／JavaScript；图标选择与生成、名称简介、解释器、开发目录、开发者信息；导入确认；删除时可选同时删除源码 | `ExtensionsTab.tsx` |
| 7 | 对话归档 | 不活跃对话归档期限（1／3／7／15 天）；清理期限（关闭／1／3／6 个月／1 年）；归档列表、标题／工作区搜索、恢复、永久删除 | 加载、空列表、无匹配、列表、恢复操作、永久删除确认 | `ArchiveTab.tsx`、`ArchivedSessionList.tsx`、`ArchivedSessionRow.tsx` |
| 8 | 关于 | Logo、应用名、版本、检查更新（开发态隐藏）、版权 | 版本有／无、开发／正式显示差异、明暗主题 | `AboutTab.tsx` |

## How

Storybook 菜单精简为 30 个展示场景，保留默认页、关键弹窗与必要状态；主题使用工具栏切换，次要变体使用 Controls。30 个原交互检查迁入 settings 同目录的 8 个 `*.test.tsx`，通过 Vitest / Testing Library 独立运行。Storybook `play` 只做视觉状态初始化。

每个选定页面先核对旧版样式及交互，按 [UI 对齐与 Storybook](../../../docs/ui-alignment.md) 制作预览。现有公开／本地组件优先复用，不引入旧版私有 `@tencent` UI 依赖。登录、模型下载、联网测试、系统设置和文件操作在预览中仅模拟。

当前预览位于 `desktop/src/renderer/src/components/settings/`：

- `SettingsLayout`、`SettingsTabLayout`、`SettingCard`、`SettingRow` 与选择器、开关、快捷键录制沿用旧版结构。侧栏 207px，菜单项间距按用户反馈由 2px 调整为 6px，分隔选中与 hover 背景；标题栏最小 44px，内容左右 24px，卡片圆角 12px，设置行最小 42px。旧版窗口尺寸参考 `xiaowei/studio/src/window/config.rs`，为 800 × 600，可调整大小。Storybook 的所有 Settings 场景使用 800 × 600 viewport；窗口在画布内水平、垂直居中，视口外框与弹窗遮罩统一按 12px 圆角裁切，空间不足时保持尺寸并滚动查看，弹窗也限制在该视口内。
- 左上角红黄绿窗口按钮仅在 `SettingsPreview` 中模拟外观，不执行关闭、最小化或全屏操作。
- 七个页面组件接收 props 和回调；`SettingsPreview.tsx` 只供 Storybook 组合展示，提供内存数据、模拟异步返回与错误，不连接 Electron／Rust／网络，不写配置或系统剪贴板。
- 相邻 `*.stories.tsx` 按 Window、General、Shortcuts、Clipboard、Models、Agent、Archive、About 分组。Storybook 可切页，也可直接选择弹窗或状态；明暗沿用当前项目主题变量，背景等设置专用样式放在 `settings.css`。没有更改产品正在使用的共享组件。
- 模型配置于 2026-09-22 按用户要求参考本机 dsh web（`http://127.0.0.1:3080/`）调整；已通过现有 Chrome 核对添加预设／自定义提供方的交互，保留本项目弹窗与视觉样式。提供统一的“添加”入口，弹窗内提供方下拉首项为“自定义”且默认选中，列表标记类型。预设提供方的地址、协议只读，自定义提供方可以编辑；预览包含 DeepSeek、OpenAI、Anthropic 三个预设，不复制 dsh 的全部提供方目录。
- 协议标识及显示统一为 `openai-completions`、`openai-responses`、`anthropic-messages`；Responses 保留自动／仅 HTTP 传输选项。获取模型后在弹窗中搜索、多选或全选当前搜索结果，点击添加合入配置列表；重复模型标为已添加，不覆盖已有配置。列表行展开编辑图片支持、上下文窗口、最大输出 token，文本支持默认开启且不显示开关。最大输出 token 提供 4K／8K／16K／32K／64K／128K 快捷档位与自定义正整数，不用原生数字步进框；档位不代表各模型支持的上限。右侧删除，底部支持手动添加模型 ID。连接信息或 Key 变化清空关联模型配置，可重新获取或手动添加。获取中禁用保存；获取失败、空结果、取消选择不会清空已有列表。
- 模型列表响应支持可选上下文上限；接口未返回且未手动配置时显示“上下文窗口：未设置”，不推测数值。单模型手动覆盖优先，否则使用接口返回值；重新获取同一连接的列表保留仍存在模型的手动配置。模型目录、下载和网络校验均为模拟，不使用真实凭据或发送请求。
- 关于页采用当前项目 Logo、示例版本，版权通过可选 prop 提供，本次预览不显示旧 Tencent 版权。
- 不提供扩展导航或占位页；剪贴板增加“清理全部”预览行，位于已用存储空间下方，包含清理中、成功及失败重试状态。未创建实际设置窗口，未注册设置入口或改变当前固定快捷键。

需要明确区分的边界：

- “清理全部”是新增入口，旧版 `ClipboardTab` 没有；用户于 2026-09-20 明确仅制作设置 UI 预览。业务语义为复用自动清理规则，将清理截止时间设为点击时的当前时刻，清理该时刻之前的全部普通数据，而非按保留天数回推截止时间。普通数据须同时满足未收藏、无备注、无标签等保护标记；任一保护标记存在即保留。业务实现暂缓，不调用现有语义不同的 `clearHistory` API，见 [剪贴板后续事项](2026-09-17-migrate-local-clipboard.md)。
- 旧版剪贴板数据库及附件迁移改为离线一次性迁移，对最终用户不可见；产品设置和 Storybook 均不提供迁移入口、进度或结果展示。迁移实现仍待后续工作，不因移除 UI 而视为已完成迁移，见 [剪贴板后续事项](2026-09-17-migrate-local-clipboard.md)。
- 旧版账号使用微信登录、UIN 和云连接状态，是否保留及如何调整待定。
- 旧版快捷键包含快速对话及自定义录制；当前产品仅有固定搜索／剪贴板快捷键，清单不表示已授权改变产品行为。
- 关于页的旧品牌资源、Tencent 版权及更新机制需在迁移该页时明确调整，不直接照搬。
- 本次只核对源码，没有取得旧版窗口截图；视觉验收仍需逐项完成。

## Current work

后续需要在 Meta KV 上实现 setting 模块，统一设置定义、默认值、校验与变更通知；本轮只更新模型配置 Storybook，不实现持久化或实际提供方接口。

已完成除扩展外的页面与弹窗预览及自动检查。用户视觉确认仍待完成；确认前不接入实际窗口或业务，事项保持 active。

本工作区 Storybook 当前使用 `http://127.0.0.1:6006/`。浏览器验证使用用户现有 Chrome，经 `agent-browser` CDP 连接。


## Outcome

- 已交付 7 个设置页面、公共框架及模型／搜索 Key／归档弹窗；共 81 个 Storybook 场景，其中 19 个带 `play` 交互检查。
- 在现有 Chrome 的 800 × 600 视口完成全部 81 个场景渲染及 19 个交互检查，没有 `storyThrewException` 或 `playFunctionThrewException`。检查覆盖页签导航、账号和开关、快捷键重复转移与取消、迁移反馈、跳转本地模型、提供商增删改与切换、协议／上下文覆盖、图片模型失败、搜索 Key 删除回退 Bing、归档搜索与删除。
- 人工检查当前预览的通用深色、快捷键浅色、剪贴板浅色、模型深色、智能体深色、归档浅色、关于深色和提供商弹窗截图。旧版运行截图未取得，不宣称已经完成用户视觉对齐验收。
- 输入测试使用 `fireEvent.change`：当前 Storybook 的 `userEvent.type` 会改变 DOM 输入值但未触发 React 受控值更新；真实 Chrome 输入已另行核验。未为了测试添加产品输入逻辑。
- `just check`、`pnpm --dir desktop build`、`pnpm --dir desktop storybook:build` 通过；新增代码显示宽度不超过 120。没有修改 Rust、启动或重启 Electron，也没有提交。
- 当前仅完成 Storybook 实现切片，对应实施 Plan 已删除；后续由用户逐页确认并决定业务接入范围。

- 2026-09-20 修正 Storybook 预览尺寸：所有 Settings 场景固定为 800 × 600，阻止底部面板挤压 viewport。Chrome 实测页面与提供商弹窗遮罩均为 800 × 600；桌面类型检查、Biome 与 Storybook 构建通过，仍待用户视觉确认。
- Settings 窗口已在画布中水平、垂直居中；Chrome 在 1440 × 1000 视口实测窗口中心与画布中心完全重合，窗口仍为 800 × 600。较小画布保留实际尺寸并可滚动查看。
- 预览外框补齐 12px 圆角，并添加 macOS 红黄绿窗口按钮的静态外观。Chrome 核验 800 × 600 尺寸、圆角和按钮位置，类型检查与 Biome 通过；仅修改预览，未接入原生窗口操作。
- 新增剪贴板“清理全部”预览行及 4 个场景，当前共 85 个场景、21 个 play 检查。Chrome 中清理成功与失败重试两项交互检查完成：成功模拟存储占用由 128 MB 降至 32 MB，失败保留原值；按钮在清理期间禁用。类型检查、Biome 和 Storybook 构建通过。仅提供 UI 与模拟反馈，真实清理和当前时刻截止语义未接入。
- 按用户要求移除“从旧版迁移数据”入口、模拟状态与 6 个专属场景；迁移方案明确为离线一次性迁移，对最终用户不可见，尚未实现。原保留期限交互检查继续保留为 `RetentionControl`。当前 Settings 共 79 个场景、21 个 play 检查。

- 2026-09-22 完成模型配置 Storybook 调整：区分预设／自定义提供方，增加显式获取模型列表、连接变更后重新获取，以及接口上下文上限与手动覆盖。新增 8 个场景，当前 Settings 共 87 个场景、27 个 play 检查。模型分组全部场景在现有 Chrome 完成渲染／交互检查，类型检查、Biome 和 Storybook 构建通过；仅预览使用模拟数据，未实现 setting 模块、持久化或真实模型请求，等待用户视觉确认。

- 2026-09-22 按用户要求移除当前提供方选择框、独立 providerId 状态和保存后的切换提供方确认；全局默认模型统一由页面上方选择器管理。删除提供方时仅清空引用该提供方的默认／小文本／图片模型，保留其他提供方的选择。移除 SwitchProvider 场景，新增配置交互改为验证保存不改变全局默认模型，再显式选择新模型；当前共 86 个场景。

- 按用户后续反馈合并为单一“添加”入口，移除独立自定义按钮；弹窗提供方下拉首项为“自定义”且默认选中，可切换预设及切回自定义。新增预设／自定义切换检查，当前 86 个场景、28 个 play 检查。

- 获取后的模型列表改为平铺行，每行右侧提供“设为默认”和上下文窗口编辑入口，替代默认模型下拉与整组折叠。Chrome 已验证新增后选择模型、行内切换默认模型以及重新获取后保留上下文覆盖；类型检查、Biome、Storybook 构建通过。

- 按用户要求移除提供方级默认上下文窗口及其预览字段，只保留逐模型手动值／接口值；原“上下文未提供”改为“上下文窗口：未设置”，表示接口未返回且用户尚未填写。

- 按用户要求移除提供方内模型行的“设为默认”及 Provider.defaultModel 字段，获取非空列表后即可保存。全局默认模型仅由页面上方设置；提供方内不再维护默认模型。

- 2026-09-22 按参考图更新模型目录及获取选择弹窗，支持搜索、多选、去重、逐模型四项配置、删除与手动添加。Chrome 通过 9 个相关交互场景，类型检查、Biome、Storybook 构建及 diff 检查通过；仅 Storybook 模拟，未接入产品业务。

- 修复获取模型弹窗替换原弹窗的问题：改为叠加显示，底层保持挂载并通过可选 inactive 状态暂停键盘及指针交互；关闭或添加后恢复原表单及行展开状态。Modal 默认行为不变。Chrome 通过叠层 Esc／Enter、上下文保留、添加及空结果四项场景，类型与构建检查通过。

- 获取模型改为立即打开顶层弹窗，在弹窗内显示加载动画、失败重试及空结果；取消使旧请求结果失效。搜索和全选合并一行，选择数量仅在添加按钮显示，移除底层加载提示。

- 精简 Settings 菜单：88 → 30 个展示场景，移除重复深色和纯交互页面；30 个独立测试全部通过，保留原流程验证覆盖；Chrome 完成保留的全部 30 个场景检查，类型检查、Biome、Storybook 构建和 diff 检查通过。

- 2026-09-23 模型配置继续调整：移除“支持文本”及其可关闭状态；最大输出 token 改用常用档位和自定义数字文本输入，避免无意义的 ±1 步进。档位参考提供方模型文档，但不当作模型能力上限校验。Storybook 800 × 600 预览完成展开布局检查；Settings 31 项测试、类型检查、Biome 和 Storybook 构建通过。只改 Storybook UI 和内存配置，产品接口未接入。
